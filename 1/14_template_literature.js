// Instead of writing string in single or double quote we will write it like back quote
// This will be help full when multiple string need to add into one template
const name = "yagnik"
const age = 5
console.log(`My name is ${name}, and my age is ${age}`)
