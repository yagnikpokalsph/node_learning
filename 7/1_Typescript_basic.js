// Typescript name itself indicated that related to type specially the statically types
// Typescript is the superset of javascript
// It adds the static typing similar to golang


// JS is the loosely typed language which means type does not matter
// TS allow the type of data being passed around the code
// Has ability to report the error when type doesn't matches
